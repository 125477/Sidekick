{
  "name": "@sidekick/extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@sidekick/core": "workspace:*"
  },
  "scripts": {
    "dev": "echo extension dev pending",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.40"
  }
}
